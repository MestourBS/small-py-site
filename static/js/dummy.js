/**
 * @param {string} text
 * @param {{[k: string]: string}} [variables]
 * @returns {string}
 */
function gettext(text, variables={}) {}
const Flask = {
    /**
     * @param {string} endpoint
     * @param {{[k: string]: string}} rule
     * @returns {string}
     */
    url_for(endpoint, rule) {}
};
